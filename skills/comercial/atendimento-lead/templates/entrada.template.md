Mensagem, canal, histórico.

## Variaveis de contexto

Carregadas de `config/empresa.yaml`:
- `empresa.nome`
- `empresa.tom`
- `empresa.vocabulario_bloqueado`

## Entradas especificas

Definidas em `schema.yaml` (campo `inputs`).
