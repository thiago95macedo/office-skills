# Exemplo avançado — Múltiplas empresas

```yaml
caminho_config: config/cliente-x.yaml
override:
  vocabulario_bloqueado: ["sigilo", "interno"]
```

Carregar configuração específica para um cliente sem alterar a base.